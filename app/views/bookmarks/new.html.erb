<h1>Add a New Movie</h1>
<div class="container">
  <%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment, placeholder: 'add comment' %>
  <%= f.association :movie, placeholder: 'add movie' %>
  <%= f.submit 'add movie', class: "btn btn-primary"%>
<% end %>
</div>
